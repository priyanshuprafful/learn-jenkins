node('workstation') {

      try {


        stage("one") {

                echo "one"
                sh 'exit 1'// to mark it as a failure ,  also sh means it is a shell command

        }

        stage("two") {

                echo "two"

        }
      } catch (e) {
              echo "This will only run if failed "
      }
}
