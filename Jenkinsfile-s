node('workstation') {

      try {


        stage("one") {

                echo "one"
                exit 1 // to mark it as a failure

        }

        stage("two") {

                echo "two"

        }
      } catch (e) {
              echo "This will only run if failed "
      }
}
